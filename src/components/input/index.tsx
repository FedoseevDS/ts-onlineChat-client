import { Template } from './styles.ts';

const Input = () => {
  return (
    <Template>
      <input placeholder='Введите название чата' type='text' />
    </Template>
  );
};

export default Input;
